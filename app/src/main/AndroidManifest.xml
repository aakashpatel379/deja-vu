<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.developer.dejavu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/game_dashboard_ico"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".display.AvatarActivity">
                    </activity>
        <activity android:name=".display.HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".display.SplashActivity">


        </activity>
        <activity android:name=".display.PlayerActivity" >

        </activity>
        <activity android:name=".display.RegisterActivity" />
        <activity android:name=".display.LoginActivity">

        </activity>
        <service android:name=".service.SoundService"/>
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/app_id" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>